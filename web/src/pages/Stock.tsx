// Stock (D-05): migrated from website/stock/ as a route stub. Functional
// migration (login + inventory fetch against the existing api/ backend) is
// deferred; this is a buildable placeholder for the /stock route.
export function Component() {
  return (
    <section>
      <h1 className="font-display text-3xl uppercase text-white">Stock</h1>
      <p className="mt-2 font-sans text-white/75">
        Inventory tool — migration in progress.
      </p>
    </section>
  )
}

export default Component
