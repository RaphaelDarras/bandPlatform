---
phase: 06.1-rework-stock-web-page-to-have-the-possibility-to-crud-produc
plan: 03
subsystem: api
tags: [fetch, jwt, react, vitest, typescript]

# Dependency graph
requires:
  - phase: 06.1-01
    provides: "GET /api/inventory/stock?includeInactive=true (active flag), POST /api/inventory/restock/batch"
  - phase: 06.1-02
    provides: "POST /api/products (409 on SKU clash), PUT /api/products/:id (variant $pull + active whitelist)"
provides:
  - "web/src/lib/inventory.ts — the project's first authenticated web API client"
  - "AuthExpiredError, thrown once from a shared authedFetch wrapper on both 401 and 403"
  - "Typed wrappers: loginAdmin, fetchStock, batchAdjustStock, createProduct, putProductVariants, deactivateProduct, restoreProduct"
affects: [06.1-04, 06.1-05, 06.1-06, 06.1-stock-page-rework]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Single authedFetch wrapper centralizes bearer-header attachment and the 401-or-403 -> AuthExpiredError contract (D-30), replacing per-call-site status checks"
    - "VITE_API_URL with Render-URL fallback (D-09), matching web/src/lib/products.ts and orders.ts"

key-files:
  created: [web/src/lib/inventory.ts, web/src/lib/inventory.test.ts]
  modified: []

key-decisions:
  - "D-30 implemented literally: both 401 and 403 throw AuthExpiredError from one location; no refresh, retry, dirty-form guard, or unsaved-changes warning added anywhere"
  - "D-27 restore reuses PUT /api/products/:id with { active: true } — no new endpoint"
  - "D-17 putProductVariants is the single variant-writing wrapper; doc comment states the caller must pass the FULL variant array or PUT will $pull (delete) omitted SKUs"
  - "loginAdmin preserves Stock.tsx's existing parse-body-then-check-ok order byte-for-byte and never throws AuthExpiredError (a wrong password is not an expired session)"

patterns-established:
  - "Authenticated web API clients live in web/src/lib/*.ts as a private authedFetch + typed exported wrappers, mirroring the existing public-client pattern in products.ts/orders.ts"

requirements-completed: [INV-05, INV-06, INV-07, INV-08]

# Metrics
duration: 25min
completed: 2026-08-06
---

# Phase 06.1 Plan 03: Authenticated Inventory API Client Summary

**Single `web/src/lib/inventory.ts` module centralizing bearer-token auth, base-URL resolution, and the 401/403-to-AuthExpiredError contract for all seven admin stock/product operations, replacing Stock.tsx's inline hardcoded-host fetch and duplicated 401/403 check.**

## Performance

- **Duration:** ~25 min
- **Completed:** 2026-08-06T17:15:41Z
- **Tasks:** 2 completed
- **Files modified:** 2 (both new)

## Accomplishments
- Built the project's first authenticated web API client (`web/src/lib/inventory.ts`), replacing the ad-hoc fetch logic currently inlined in `Stock.tsx`
- Implemented D-30's binding contract in exactly one place: `authedFetch` throws `AuthExpiredError` on both 401 and 403, guarding against the exact drift that caused the mobile app's apiClient bug (handled 401 only, forgot 403)
- Exported typed wrappers for all seven authenticated operations this phase's page rework will need, plus the one unauthenticated `loginAdmin` call
- Wrote 13 unit tests covering both auth-failure codes per applicable call site, the D-06 "ordinary error never logs out" distinction (409 and login-401), payload/header shape, and the D-17 full-array length guard

## Task Commits

Each task was committed atomically:

1. **Task 1: Write the authenticated client module** - `f82f93d` (feat)
2. **Task 2: Unit-test the client, both auth-failure codes included** - `6b1307f` (test)

**Plan metadata:** SUMMARY commit follows this file.

## Files Created/Modified
- `web/src/lib/inventory.ts` - authedFetch (401/403 -> AuthExpiredError, else res.ok-throw with error-body unwrap, else res.json), AuthExpiredError class, StockVariant/StockProduct/StockData/BatchAdjustment/BatchResult/VariantPayload/CreateProductInput types, and loginAdmin/fetchStock/batchAdjustStock/createProduct/putProductVariants/deactivateProduct/restoreProduct wrappers
- `web/src/lib/inventory.test.ts` - 13 tests: separate 401 and 403 AuthExpiredError assertions for fetchStock and batchAdjustStock, 409/login-401 plain-Error-not-AuthExpiredError assertions, unparseable-body fallback, header/payload shape for every wrapper, and a D-17 variants-array-length guard on putProductVariants

## Decisions Made
- No deviations from the plan's specified decisions; D-30, D-27, D-17, D-09, and D-29 were all implemented exactly as quoted in the plan/context.
- Two minor wording edits to source comments (spelling out "API-base-URL-env-var" and "bearer auth header" instead of the literal strings `VITE_API_URL`/`Authorization`) were needed so the plan's single-match grep acceptance criteria held while still documenting intent in prose — this is not a functional deviation, just comment phrasing to satisfy an exact-match verification rule.

## Deviations from Plan

None - plan executed exactly as written. The only adjustment was the doc-comment wording above, made proactively while satisfying the plan's own acceptance criteria (not a Rule 1-4 deviation, since no code behavior changed).

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- `web/src/lib/inventory.ts` is ready to be imported by the reworked `Stock.tsx` (or its successor components) for login, stock fetch, batch restock, product create/update/deactivate/restore
- No page was modified by this plan (by design) — the next plan(s) in this phase wire this client into the UI and remove `Stock.tsx`'s inline hardcoded host and duplicated 401/403 handling
- Full web suite verified green (139/139 tests, 26 files) and `tsc --noEmit` clean before finishing, confirming no regression to existing lib modules

---
*Phase: 06.1-rework-stock-web-page-to-have-the-possibility-to-crud-produc*
*Completed: 2026-08-06*
