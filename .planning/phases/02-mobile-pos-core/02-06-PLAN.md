---
phase: 02-mobile-pos-core
plan: 06
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-04", "02-05"]
files_modified:
  - mobile/src/app/concerts/index.tsx
  - mobile/src/app/concerts/[id].tsx
  - mobile/src/app/concerts/new.tsx
  - mobile/src/features/concerts/useConcerts.ts
  - mobile/src/features/concerts/ConcertList.tsx
  - mobile/src/features/concerts/ConcertSummary.tsx
  - mobile/src/app/history/[saleId].tsx
  - mobile/src/app/(tabs)/history.tsx
  - mobile/src/features/history/SaleList.tsx
  - mobile/src/features/history/SaleDetail.tsx
  - mobile/src/features/history/useHistory.ts
autonomous: true
requirements: [POS-03, POS-04]
must_haves:
  truths:
    - "User can view list of concerts sorted by date"
    - "User can quick-create a concert with name, venue, date, city"
    - "User can close a concert and see totals summary"
    - "User can view transaction history grouped by concert"
    - "User can filter history by concert"
    - "User can tap a sale to see full detail with void/unvoid button"
    - "Voided sales show VOIDED badge and can be unvoided"
    - "Per-concert price overrides can be set before/during a concert"
  artifacts:
    - path: "mobile/src/app/concerts/index.tsx"
      provides: "Concert list screen"
      min_lines: 20
    - path: "mobile/src/app/concerts/new.tsx"
      provides: "Quick-create concert form"
      min_lines: 30
    - path: "mobile/src/app/concerts/[id].tsx"
      provides: "Concert detail with close button and price overrides"
      min_lines: 40
    - path: "mobile/src/app/(tabs)/history.tsx"
      provides: "Transaction history tab screen"
      min_lines: 30
    - path: "mobile/src/app/history/[saleId].tsx"
      provides: "Sale detail screen with void/unvoid"
      min_lines: 40
  key_links:
    - from: "mobile/src/features/concerts/useConcerts.ts"
      to: "mobile/src/api/client.ts"
      via: "Fetch concerts from API, cache locally"
      pattern: "api.*concerts"
    - from: "mobile/src/features/concerts/useConcerts.ts"
      to: "mobile/src/db/concerts.ts"
      via: "Cache concerts in SQLite for offline access"
      pattern: "cacheConcerts"
    - from: "mobile/src/features/history/useHistory.ts"
      to: "mobile/src/db/sales.ts"
      via: "Read local sales from SQLite"
      pattern: "getLocalSales"
    - from: "mobile/src/app/history/[saleId].tsx"
      to: "mobile/src/db/sales.ts"
      via: "voidLocalSale / unvoidLocalSale"
      pattern: "voidLocalSale|unvoidLocalSale"
---

<objective>
Build concert management screens and transaction history with void/unvoid capability.

Purpose: Concerts are the organizing unit for POS sales. Band members need to create concerts for gigs, manage price overrides, and review sale history with the ability to void incorrect transactions.
Output: Concert CRUD screens, price override management, transaction history with grouping and void/unvoid.
</objective>

<execution_context>
@C:/Users/rapha/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rapha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-mobile-pos-core/02-CONTEXT.md
@.planning/phases/02-mobile-pos-core/02-RESEARCH.md
@.planning/phases/02-mobile-pos-core/02-01-SUMMARY.md
@.planning/phases/02-mobile-pos-core/02-03-SUMMARY.md
@.planning/phases/02-mobile-pos-core/02-05-SUMMARY.md

<interfaces>
<!-- From Plan 02-01 (API) -->
GET /api/concerts -> Concert[]
POST /api/concerts -> { name, date, venue, location } -> Concert
GET /api/concerts/:id -> Concert
PATCH /api/concerts/:id -> { active: boolean, ... } -> Concert

<!-- From Plan 02-02 (API) -->
GET /api/sales?concertId=xxx -> Sale[]
POST /api/sales/:id/void -> Sale (with voidedAt set)
POST /api/sales/:id/unvoid -> Sale (with voidedAt cleared)

<!-- From Plan 02-03 (data layer) -->
From mobile/src/db/concerts.ts:
- cacheConcerts(db, concerts[])
- getCachedConcerts(db)
- setPriceOverride(db, concertId, productId, price)
- getPriceOverrides(db, concertId)
- clearPriceOverrides(db, concertId)

From mobile/src/db/sales.ts:
- getLocalSales(db, concertId?)
- voidLocalSale(db, saleId)
- unvoidLocalSale(db, saleId)

From mobile/src/stores/cartStore.ts:
- useCartStore.setConcertId(id)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Concert Management Screens</name>
  <files>mobile/src/features/concerts/useConcerts.ts, mobile/src/features/concerts/ConcertList.tsx, mobile/src/features/concerts/ConcertSummary.tsx, mobile/src/app/concerts/index.tsx, mobile/src/app/concerts/new.tsx, mobile/src/app/concerts/[id].tsx</files>
  <action>
    Create mobile/src/features/concerts/useConcerts.ts:
    - Hook that manages concert data
    - fetchConcerts(): if online, GET /api/concerts and cache to SQLite. If offline, read from SQLite cache.
    - createConcert(data): POST /api/concerts with { name, date, venue, location }. On success, update local cache. Requires online (per user decision: "product changes are not queued offline" — same logic for concerts).
    - closeConcert(id): PATCH /api/concerts/:id with { active: false }. Triggers final sync. Returns summary data.
    - reopenConcert(id): PATCH /api/concerts/:id with { active: true }. (Claude's discretion: allow reopening)
    - Returns: concerts array, loading, error, refresh function

    Create mobile/src/features/concerts/ConcertList.tsx:
    - FlatList of concerts sorted by date descending
    - Each row: concert name, venue, date (formatted), city, active/closed status badge
    - Active concerts at top, closed ones below
    - Tap: navigates to concert detail [id]

    Create mobile/src/features/concerts/ConcertSummary.tsx:
    - Component showing concert totals (displayed when closing a concert):
      - Total revenue (sum of all sale totalAmounts for this concert)
      - Number of transactions
      - Total items sold
    - Clean card layout, prominent numbers

    Create mobile/src/app/concerts/index.tsx:
    - Screen: renders ConcertList
    - Header: "Concerts" with "+" button to navigate to new.tsx
    - Pull-to-refresh

    Create mobile/src/app/concerts/new.tsx:
    - Quick-create form with 4 required fields (per user decision):
      1. Name (text input)
      2. Venue (text input)
      3. Date (date picker — use system date picker)
      4. City (text input) — maps to Concert.location field
    - "Create Concert" button
    - Validation: all fields required, show inline errors
    - On success: navigate to the created concert's detail page
    - Requires online — show "Connect to internet to create concerts" if offline

    Create mobile/src/app/concerts/[id].tsx:
    - Concert detail screen:
      - Concert info: name, venue, date, city
      - Status: Active / Closed badge
      - "Start Selling" button — sets cartStore.setConcertId(id), navigates to selling mode
      - If active: "Close Concert" button (red/warning). On tap:
        1. Trigger sync of any pending outbox items for this concert
        2. PATCH concert to active: false
        3. Show ConcertSummary with totals
        4. Clear price overrides for this concert
      - If closed: "Reopen Concert" button (Claude's discretion: allowed)
      - Per-concert price overrides section:
        - List of products with current price and override field
        - Tap a product to set override price for this concert
        - "Reset Prices" button to clear all overrides
        - Overrides stored in SQLite concert_price_overrides table
        - Price overrides only apply to this concert (per user decision: "Price overrides reset each new concert")
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Concert list shows all concerts. Quick-create form with 4 fields works online. Concert detail has Start Selling, Close Concert (with summary), and price override management. Closed concerts can be reopened.</done>
</task>

<task type="auto">
  <name>Task 2: Transaction History + Sale Detail + Void</name>
  <files>mobile/src/features/history/useHistory.ts, mobile/src/features/history/SaleList.tsx, mobile/src/features/history/SaleDetail.tsx, mobile/src/app/(tabs)/history.tsx, mobile/src/app/history/[saleId].tsx</files>
  <action>
    Create mobile/src/features/history/useHistory.ts:
    - Hook managing transaction history data
    - loadHistory(concertId?): reads from local SQLite sales table. If concertId provided, filter by it.
    - groupByConcert(sales): groups sales by concert_id, returns sections with concert header + sales array
    - Returns: sections (grouped), loading, selectedConcertId, setSelectedConcertId, refresh

    Create mobile/src/features/history/SaleList.tsx:
    - SectionList component (per user decision: "Grouped by concert (headers), chronological within each concert (newest first)")
    - Section headers: concert name + date
    - Each sale row:
      - Time (formatted from created_at)
      - Item count summary (e.g., "3 items")
      - Total amount + currency
      - Payment method badge
      - If voided: "VOIDED" badge (per user decision: "Voided sales remain visible with VOIDED badge")
      - Synced indicator (checkmark if synced to server)
    - Tap: navigates to sale detail [saleId]

    Create mobile/src/features/history/SaleDetail.tsx:
    - Full sale breakdown component (per user decision):
      - All items with: product name, variant info, quantity, price per unit, line total
      - Discount applied (if any): amount and type
      - Total amount
      - Payment method
      - Currency
      - Timestamp (formatted date + time)
      - Sync status
    - Void/Unvoid button:
      - If not voided: "Void Sale" button (red/destructive). Shows confirmation dialog before voiding.
      - If voided: "Unvoid Sale" button (restores sale)
      - Void action: voidLocalSale(db, saleId) + add outbox entry for server sync (type: 'sale_void')
      - Unvoid action: unvoidLocalSale(db, saleId) + add outbox entry (type: 'sale_unvoid')
      - Both update local stock optimistically

    Update mobile/src/app/(tabs)/history.tsx:
    - Replace placeholder with actual history screen
    - Concert filter dropdown at top (per user decision: "Filter by concert only")
    - Renders SaleList component
    - Pull-to-refresh

    Create mobile/src/app/history/[saleId].tsx:
    - Screen wrapping SaleDetail component
    - Loads sale from local SQLite by ID
    - Back navigation to history list
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit 2>&1 | head -20 && npm test -- --testPathPattern=features/history --passWithNoTests 2>&1 | tail -10</automated>
  </verify>
  <done>History tab shows sales grouped by concert with newest first. Concert filter dropdown works. Sale detail shows full breakdown. Void/unvoid writes to local DB + outbox. VOIDED badge visible on voided sales.</done>
</task>

</tasks>

<verification>
```bash
cd mobile && npx tsc --noEmit
```
TypeScript compiles clean. Visual verification: concerts can be created/listed/closed, history shows grouped sales, void/unvoid works on sale detail.
</verification>

<success_criteria>
- Concert list shows concerts sorted by date with active/closed status
- Quick-create with 4 required fields (name, venue, date, city) works online
- Concert detail has Start Selling, Close Concert with summary, price overrides
- Concert close triggers sync and shows totals (revenue, transactions, items sold)
- Per-concert price overrides stored in SQLite, reset on new concert
- Transaction history grouped by concert headers, newest first within each
- Concert filter dropdown in history screen
- Sale detail shows all items, prices, discount, payment method, currency, timestamp
- Void/Unvoid button on sale detail with confirmation dialog
- Voided sales show "VOIDED" badge, remain visible in list
- Void/unvoid writes to local DB and creates outbox entry for server sync
</success_criteria>

<output>
After completion, create `.planning/phases/02-mobile-pos-core/02-06-SUMMARY.md`
</output>
