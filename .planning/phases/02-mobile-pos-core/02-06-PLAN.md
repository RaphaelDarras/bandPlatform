---
phase: 02-mobile-pos-core
plan: 06
type: execute
wave: 3
depends_on: [02-01, 02-02, 02-04, 02-05]
files_modified:
  - mobile/src/app/concerts/index.tsx
  - mobile/src/app/concerts/[id].tsx
  - mobile/src/app/concerts/new.tsx
  - mobile/src/app/(tabs)/history.tsx
  - mobile/src/app/history/[saleId].tsx
  - mobile/src/features/concerts/useConcerts.ts
  - mobile/src/features/history/useHistory.ts
  - mobile/src/api/concerts.ts
  - mobile/src/api/sales.ts
  - mobile/src/db/concerts.ts
  - mobile/src/__tests__/features/history/history.test.ts
autonomous: true
requirements: [POS-03]
must_haves:
  truths:
    - "User can view list of concerts and create new ones (name, venue, date, city)"
    - "User can close a concert and see totals summary"
    - "User can view transaction history grouped by concert, newest first"
    - "User can tap a sale to see full detail with void/unvoid button"
    - "Voided sales show VOIDED badge and can be unvoided"
    - "User can filter transaction history by concert"
  artifacts:
    - path: "mobile/src/app/concerts/index.tsx"
      provides: "Concert list screen with + New Concert button"
    - path: "mobile/src/app/concerts/[id].tsx"
      provides: "Concert detail with close button and totals"
    - path: "mobile/src/app/concerts/new.tsx"
      provides: "Quick-create concert form (name, venue, date, city)"
    - path: "mobile/src/app/(tabs)/history.tsx"
      provides: "Transaction history tab with concert grouping"
    - path: "mobile/src/app/history/[saleId].tsx"
      provides: "Sale detail screen with void/unvoid"
    - path: "mobile/src/features/history/useHistory.ts"
      provides: "Hook for loading transaction history from local SQLite"
      exports: ["useHistory"]
    - path: "mobile/src/features/concerts/useConcerts.ts"
      provides: "Hook for concert management"
      exports: ["useConcerts"]
    - path: "mobile/src/__tests__/features/history/history.test.ts"
      provides: "Unit tests for history logic"
  key_links:
    - from: "mobile/src/features/concerts/useConcerts.ts"
      to: "mobile/src/api/concerts.ts"
      via: "API calls for concert CRUD when online"
      pattern: "apiClient.*(get|post|patch).*concerts"
    - from: "mobile/src/features/history/useHistory.ts"
      to: "mobile/src/db/sales.ts"
      via: "Local SQLite query for sales by concert"
      pattern: "getLocalSales"
    - from: "mobile/src/app/history/[saleId].tsx"
      to: "mobile/src/db/sales.ts"
      via: "voidLocalSale / unvoidLocalSale for void actions"
      pattern: "(void|unvoid)LocalSale"
---

<objective>
Build concert management screens (list, create, close) and transaction history with void/unvoid capability.

Purpose: Concert management scopes selling sessions. Transaction history enables sales auditing and error correction (void/unvoid) during events.
Output: Concert CRUD screens, transaction history with grouping and filtering, sale detail with void/unvoid.
</objective>

<execution_context>
@C:/Users/rapha/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rapha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-mobile-pos-core/02-CONTEXT.md
@.planning/phases/02-mobile-pos-core/02-RESEARCH.md
@.planning/phases/02-mobile-pos-core/02-03-SUMMARY.md
@.planning/phases/02-mobile-pos-core/02-04-SUMMARY.md
@.planning/phases/02-mobile-pos-core/02-05-SUMMARY.md

<interfaces>
From mobile/src/db/sales.ts:
```typescript
export function getLocalSales(db: SQLiteDatabase, concertId?: string): Promise<LocalSale[]>;
export function voidLocalSale(db: SQLiteDatabase, id: string): Promise<void>;
export function unvoidLocalSale(db: SQLiteDatabase, id: string): Promise<void>;
```

From mobile/src/db/outbox.ts:
```typescript
export function recordSaleLocally(db, sale, outboxEntry): Promise<void>;
// For void outbox entries too
```

From mobile/src/stores/cartStore.ts:
```typescript
export const useCartStore; // setConcertId, clearCart
```

From mobile/src/api/client.ts:
```typescript
export const apiClient: AxiosInstance; // JWT-authenticated
```

API endpoints (from Plans 01+02):
- GET /api/concerts — list all concerts
- POST /api/concerts — create { name, venue, date, city }
- GET /api/concerts/:id — detail
- PATCH /api/concerts/:id — close/reopen { active: false|true }
- GET /api/sales?concertId=xxx — list sales with filter
- POST /api/sales/:id/void — void a sale
- POST /api/sales/:id/unvoid — unvoid a sale
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Concert management screens (list, create, detail/close)</name>
  <files>mobile/src/app/concerts/index.tsx, mobile/src/app/concerts/[id].tsx, mobile/src/app/concerts/new.tsx, mobile/src/features/concerts/useConcerts.ts, mobile/src/api/concerts.ts, mobile/src/db/concerts.ts</files>
  <action>
    Create mobile/src/api/concerts.ts:
    - apiGetConcerts(): GET /concerts, returns Concert[]
    - apiCreateConcert(data): POST /concerts with { name, venue, date, city }, returns Concert
    - apiGetConcert(id): GET /concerts/:id
    - apiPatchConcert(id, data): PATCH /concerts/:id with partial update

    Create mobile/src/db/concerts.ts:
    - getCachedConcerts(db): SELECT * FROM concerts ORDER BY date DESC
    - upsertConcerts(db, concerts): INSERT OR REPLACE for caching API response
    - getConcertById(db, id): single concert lookup

    Create mobile/src/features/concerts/useConcerts.ts:
    - useConcerts hook:
      - concerts: list loaded from SQLite cache
      - loadConcerts: if online, fetch from API + upsert to cache; if offline, read from cache
      - createConcert: if online, POST to API + upsert locally; if offline, create in SQLite + add outbox entry
      - closeConcert(id): PATCH API { active: false } + update local cache + trigger final sync + return totals
      - reopenConcert(id): PATCH API { active: true } + update local cache (Claude's discretion: allow reopen)
      - activeConcert: currently active concert (used for selling session context)

    Create mobile/src/app/concerts/index.tsx:
    - FlatList of concerts sorted by date desc
    - Each row shows: name, venue, date, city, status badge (active/closed)
    - "+" button at top-right to create new concert
    - Tap row navigates to concerts/[id]
    - Pull-to-refresh to sync with API

    Create mobile/src/app/concerts/new.tsx:
    - Form with 4 required fields: name, venue, date, city
    - Date picker for date field
    - "Create" button calls useConcerts.createConcert
    - On success, navigates back to concert list
    - Validates all 4 fields non-empty before submit

    Create mobile/src/app/concerts/[id].tsx:
    - Concert detail: name, venue, date, city, status
    - If active:
      - "Start Selling" button: sets cartStore.concertId and navigates to selling/
      - "Close Concert" button: calls closeConcert(id), shows totals summary modal (total revenue, transaction count, items sold)
    - If closed:
      - "Reopen Concert" button (Claude's discretion: allows reopening)
      - Totals summary always visible
    - Per-concert price overrides section: list of overridden products with prices, "Edit Prices" button to add/modify overrides (stored in SQLite concert_price_overrides table)
    - Auto-resume: if app opens and last concert is still active, dashboard "Resume Selling" card takes user here (Claude's discretion)
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit && npm test -- --passWithNoTests</automated>
  </verify>
  <done>Concert list shows all concerts, create form works with 4 fields, detail shows status with start selling / close actions, per-concert price overrides editable</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Transaction history tab and sale detail with void/unvoid</name>
  <files>mobile/src/app/(tabs)/history.tsx, mobile/src/app/history/[saleId].tsx, mobile/src/features/history/useHistory.ts, mobile/src/api/sales.ts, mobile/src/__tests__/features/history/history.test.ts</files>
  <behavior>
    useHistory:
    - getSalesGroupedByConcert returns sales grouped by concert with concert name headers, newest first within each group
    - sales include voided flag and all item details

    History screen:
    - SectionList with concert name headers
    - Each sale row shows: time, item count, total, payment method, VOIDED badge if applicable
    - Filter dropdown to select specific concert

    Sale detail:
    - Shows all items with quantities, variant info, prices
    - Shows discount, total, payment method, currency, timestamp
    - Void button (if not voided): voids locally in SQLite + adds outbox entry for API sync
    - Unvoid button (if voided): unvoids locally + adds outbox entry
    - Stock adjusted locally on void (add back) and unvoid (re-deduct)
  </behavior>
  <action>
    Create mobile/src/api/sales.ts:
    - apiGetSales(concertId?: string): GET /sales?concertId=xxx
    - apiVoidSale(id): POST /sales/:id/void
    - apiUnvoidSale(id): POST /sales/:id/unvoid

    Create mobile/src/features/history/useHistory.ts:
    - useHistory hook:
      - salesByGroup: Record<string, LocalSale[]> grouped by concertId
      - concertNames: map of concertId -> concert name (from cached concerts)
      - loadHistory(concertFilter?: string): query SQLite sales table, group by concert, sort newest first
      - voidSale(saleId): voidLocalSale in SQLite + add outbox entry (type='sale_void') + reverse local stock + if online, call apiVoidSale
      - unvoidSale(saleId): unvoidLocalSale + outbox entry (type='sale_unvoid') + re-deduct local stock + if online, call apiUnvoidSale
    - Voided sales remain in the list with voided=true flag

    Create mobile/src/app/(tabs)/history.tsx:
    - SectionList: sections = concerts, items = sales within each concert
    - Section header: concert name + date
    - Sale row: timestamp (formatted with date-fns), item count, total amount, currency, payment method icon
    - If sale.voided: show "VOIDED" badge (red text) on the row
    - Concert filter: dropdown at top, default "All Concerts"
    - Pull-to-refresh to reload from SQLite
    - Tap sale row navigates to history/[saleId]

    Create mobile/src/app/history/[saleId].tsx:
    - Load sale from SQLite by ID
    - Display: all items with product name, variant, quantity, price per item, line total
    - Display: discount (amount + type), subtotal, grand total
    - Display: payment method, currency, timestamp
    - If NOT voided: "Void Sale" button (red). On tap: confirm dialog -> voidSale(id). Show "VOIDED" badge after void.
    - If voided: "Unvoid Sale" button (blue). On tap: confirm dialog -> unvoidSale(id). Remove "VOIDED" badge after unvoid.
    - Both void and unvoid create outbox entries for background sync

    Write mobile/src/__tests__/features/history/history.test.ts:
    - Test: getSalesGroupedByConcert groups correctly by concertId
    - Test: sales within each group sorted by created_at desc (newest first)
    - Test: voidSale calls voidLocalSale + creates outbox entry + adjusts stock
    - Test: unvoidSale calls unvoidLocalSale + creates outbox entry + adjusts stock
    - Test: voided sales have voided=true flag
  </action>
  <verify>
    <automated>cd mobile && npm test -- --testPathPattern="features/history" --verbose</automated>
  </verify>
  <done>History tab shows sales grouped by concert with newest first, filter by concert works, sale detail shows full breakdown, void/unvoid buttons work with local stock adjustment and outbox entry</done>
</task>

</tasks>

<verification>
- `cd mobile && npx tsc --noEmit` — compiles
- `cd mobile && npm test` — history tests pass
- Concert list, create, and close flows work
- Transaction history shows grouped sales with VOIDED badges
- Void/unvoid adjusts local stock and creates outbox entries
- Filter by concert works in history tab
</verification>

<success_criteria>
- Concert CRUD: list, create (4 fields), detail, close with totals summary (POS-03 support)
- Per-concert price overrides stored in SQLite
- Transaction history grouped by concert, chronological (newest first) (POS-03)
- Concert filter dropdown on history
- Voided sales visible with "VOIDED" badge, can be unvoided
- Void/unvoid creates outbox entries for background sync
- Sale detail shows all items, variant info, discount, total, payment method, currency
</success_criteria>

<output>
After completion, create `.planning/phases/02-mobile-pos-core/02-06-SUMMARY.md`
</output>
