---
phase: 02-mobile-pos-core
plan: 05
type: execute
wave: 2
depends_on: ["02-03"]
files_modified:
  - mobile/src/app/selling/_layout.tsx
  - mobile/src/app/selling/index.tsx
  - mobile/src/app/selling/review.tsx
  - mobile/src/features/catalog/ProductGrid.tsx
  - mobile/src/features/catalog/ProductTile.tsx
  - mobile/src/features/catalog/VariantPicker.tsx
  - mobile/src/features/cart/CartBar.tsx
  - mobile/src/features/cart/CartSheet.tsx
  - mobile/src/features/catalog/useCatalog.ts
  - mobile/src/features/cart/useCart.ts
autonomous: true
requirements: [POS-02, POS-09, POS-10, POS-01]
must_haves:
  truths:
    - "User can see product grid with tiles showing name, price, and per-variant stock counts"
    - "Tapping single-variant product adds directly to cart"
    - "Tapping multi-variant product opens bottom sheet with variant stock counts"
    - "Cart bar at bottom shows item count and running total"
    - "Tapping cart bar expands full cart with quantity controls"
    - "Review screen shows all items, total, payment method, currency, discount"
    - "Confirming sale writes to SQLite + outbox atomically"
    - "After sale: toast shown, cart cleared, returns to product grid"
  artifacts:
    - path: "mobile/src/features/catalog/ProductGrid.tsx"
      provides: "Grid of product tiles"
      min_lines: 30
    - path: "mobile/src/features/catalog/VariantPicker.tsx"
      provides: "Bottom sheet variant selector"
      min_lines: 40
    - path: "mobile/src/features/cart/CartBar.tsx"
      provides: "Sticky bottom cart bar"
      min_lines: 20
    - path: "mobile/src/app/selling/review.tsx"
      provides: "Sale review and confirmation screen"
      min_lines: 60
    - path: "mobile/src/features/catalog/useCatalog.ts"
      provides: "Catalog data hook with price overrides"
      min_lines: 15
    - path: "mobile/src/features/cart/useCart.ts"
      provides: "Cart hook wrapping store + sale recording"
      min_lines: 30
  key_links:
    - from: "mobile/src/features/catalog/ProductTile.tsx"
      to: "mobile/src/stores/cartStore.ts"
      via: "addItem on tap"
      pattern: "addItem"
    - from: "mobile/src/features/catalog/VariantPicker.tsx"
      to: "@gorhom/bottom-sheet"
      via: "BottomSheet component for variant display"
      pattern: "BottomSheet"
    - from: "mobile/src/app/selling/review.tsx"
      to: "mobile/src/db/outbox.ts"
      via: "recordSaleLocally on confirm"
      pattern: "recordSaleLocally"
    - from: "mobile/src/app/selling/_layout.tsx"
      to: "mobile/src/features/cart/CartBar.tsx"
      via: "Cart bar replaces tab bar in selling layout"
      pattern: "CartBar"
---

<objective>
Build the complete selling flow: product grid, variant picker, cart management, and sale recording with offline persistence.

Purpose: This is the core POS functionality. Band members select products, build a multi-item transaction, review, and confirm. Sales are written atomically to local SQLite + outbox, surviving crashes and working fully offline.
Output: Complete selling flow from product selection to sale confirmation with offline persistence.
</objective>

<execution_context>
@C:/Users/rapha/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rapha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-mobile-pos-core/02-CONTEXT.md
@.planning/phases/02-mobile-pos-core/02-RESEARCH.md
@.planning/phases/02-mobile-pos-core/02-03-SUMMARY.md

<interfaces>
<!-- From Plan 02-03 (data layer) -->
From mobile/src/stores/cartStore.ts:
- useCartStore: { items, concertId, currency, discount, discountType, addItem, removeItem, updateQuantity, setCurrency, setDiscount, clearCart, total(), itemCount() }
- CartItem: { productId, variantSku, name, variantLabel, quantity, priceAtSale }

From mobile/src/db/outbox.ts:
- recordSaleLocally(db, sale, outboxEntry): atomic write to sales + outbox tables

From mobile/src/db/products.ts:
- getCachedProducts(db): returns cached product catalog from SQLite

From mobile/src/db/concerts.ts:
- getPriceOverrides(db, concertId): returns price overrides for current concert

From mobile/src/utils/currency.ts:
- formatCurrency(amount, currencyCode): formatted currency string
- CURRENCIES: array of { code, symbol }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Product Grid + Variant Picker</name>
  <files>mobile/src/features/catalog/ProductGrid.tsx, mobile/src/features/catalog/ProductTile.tsx, mobile/src/features/catalog/VariantPicker.tsx, mobile/src/features/catalog/useCatalog.ts</files>
  <action>
    Create mobile/src/features/catalog/useCatalog.ts:
    - Hook that loads products from local SQLite cache (getCachedProducts)
    - Applies concert price overrides if active concert has overrides
    - Returns products array, loading state, refresh function
    - Products include computed totalStock (sum of all variant stocks) and effective price (base + override)

    Create mobile/src/features/catalog/ProductTile.tsx:
    - Props: product object, onTap callback
    - Display per user decision:
      - Product name (bold, top)
      - Image thumbnail (use expo-image with placeholder when offline — gray icon placeholder)
      - Price (basePrice, or overridden price if concert override exists)
      - Per-variant stock counts directly on tile (e.g., "S:3 M:12 L:0 XL:5") — user specifically requested this: "Grid tiles must show per-variant stock counts directly"
      - If only 1 variant: show just total stock number
    - Visual: NativeWind card with rounded corners, shadow, padding
    - Touch: Pressable with opacity feedback
    - Zero-stock variants shown but dimmed (sale still goes through per user decision)

    Create mobile/src/features/catalog/ProductGrid.tsx:
    - FlatList with numColumns={2} for grid layout
    - Renders ProductTile for each active product
    - On tile tap:
      - If product has 1 variant: call addItem directly to cart store with that variant's details
      - If product has multiple variants: open VariantPicker bottom sheet
    - Empty state: "No products cached. Connect to sync." message
    - Uses useCatalog hook for data

    Create mobile/src/features/catalog/VariantPicker.tsx:
    - Uses @gorhom/bottom-sheet (per user decision — do NOT hand-roll)
    - Shows product name at top of sheet
    - List of variants, each showing:
      - Variant label (size + color, e.g., "Large / Black")
      - SKU
      - Individual stock count (e.g., "3 left") — user specifically requested: "each variant with its individual stock count"
      - Price (base + priceAdjustment)
      - Zero-stock variants shown but dimmed with "0 left"
    - Tap on a variant: adds to cart store, closes bottom sheet
    - Backdrop press: closes sheet without adding
    - Snap points: ['40%', '70%'] depending on variant count
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit 2>&1 | head -20 && npm test -- --testPathPattern=features/catalog --passWithNoTests 2>&1 | tail -10</automated>
  </verify>
  <done>Product grid renders tiles with per-variant stock counts. Single-variant products add to cart on tap. Multi-variant products open bottom sheet picker. All variant stock counts visible.</done>
</task>

<task type="auto">
  <name>Task 2: Cart Bar + Review Screen + Sale Recording</name>
  <files>mobile/src/features/cart/CartBar.tsx, mobile/src/features/cart/CartSheet.tsx, mobile/src/features/cart/useCart.ts, mobile/src/app/selling/_layout.tsx, mobile/src/app/selling/index.tsx, mobile/src/app/selling/review.tsx</files>
  <action>
    Create mobile/src/features/cart/useCart.ts:
    - Wraps cartStore with sale recording logic
    - confirmSale(): async function that:
      1. Gets cart state (items, concertId, currency, discount, discountType, total)
      2. Generates UUID for sale ID
      3. Generates idempotency_key: `sale_create_${saleId}`
      4. Calls recordSaleLocally(db, sale, outboxEntry) — atomic SQLite write
      5. Updates local stock counts in products_cache (optimistic decrement per user decision)
      6. Clears cart via cartStore.clearCart()
      7. Returns the sale ID
    - Handles payment method selection state
    - Exposes all cartStore actions + confirmSale

    Create mobile/src/features/cart/CartBar.tsx:
    - Sticky bottom bar (per user decision: "Sticky bottom bar in portrait mode showing item count + total")
    - Shows: item count badge + formatted total amount + currency code
    - Tap: navigates to full cart sheet OR expands CartSheet
    - Visually prominent: colored background, large text
    - Only visible when items.length > 0

    Create mobile/src/features/cart/CartSheet.tsx:
    - Full cart view (expanded from cart bar or as a modal)
    - Shows each cart item: name, variant label, quantity, price per unit, line total
    - Quantity controls: minus (-) button, quantity display, plus (+) button
    - Remove button (swipe or X icon)
    - Subtotal at bottom
    - "Review Sale" button at bottom to navigate to review screen

    Create mobile/src/app/selling/_layout.tsx:
    - Selling mode layout (per user decision: "cart bar replaces the bottom tab bar navigation")
    - Stack navigator with: product grid (index) and review screen
    - CartBar rendered at bottom of this layout (replaces tab bar)
    - Back arrow in header top-left to exit selling mode (per user decision)
    - Exiting selling mode: navigates back to dashboard (Claude's discretion: dashboard, not concert detail)

    Create mobile/src/app/selling/index.tsx:
    - Renders ProductGrid component
    - Header shows active concert name
    - Entry point: reached from Dashboard "Start Selling" card

    Create mobile/src/app/selling/review.tsx:
    - Sale review screen (per user decision: "Full review screen showing all items, quantities, total, payment method selection, currency, and discount before final confirm"):
      1. All items list with quantities and prices (read-only, not editable here)
      2. Subtotal display
      3. Discount field: numeric input + toggle between flat ($) and percent (%) (per user decision)
      4. Final total (after discount) prominently displayed
      5. Payment method selector: Cash, Card, E-transfer, PayPal (per user decision — radio buttons or segmented control)
      6. Currency selector: tappable field showing current currency code, opens picker with configured currencies (per user decision: "Per-sale currency selection. EUR as default")
      7. "Confirm Sale" button — calls useCart().confirmSale()
    - On confirm:
      1. Call confirmSale() which writes to SQLite + outbox atomically
      2. Show brief success toast (per user decision: "Brief success toast, cart resets, immediately back to product grid")
      3. Navigate back to selling/index (product grid)
    - Confirm button disabled while processing (prevent double-tap)
    - Use Alert.alert or react-native toast library for success feedback
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit 2>&1 | head -20 && npm test -- --testPathPattern="features/(cart|catalog)" --passWithNoTests 2>&1 | tail -10</automated>
  </verify>
  <done>Complete selling flow: product grid -> variant picker -> cart bar -> review screen -> confirm -> SQLite write -> toast -> back to grid. Payment method, currency, and discount all selectable on review screen. Sale persists atomically to local storage.</done>
</task>

</tasks>

<verification>
```bash
cd mobile && npx tsc --noEmit
```
TypeScript compiles clean. Visual verification flow: enter selling mode -> tap products -> see cart bar update -> review sale -> select payment/currency/discount -> confirm -> toast -> cart reset.
</verification>

<success_criteria>
- Product grid shows tiles with per-variant stock counts on each tile
- Single-variant products add to cart on tap (no picker)
- Multi-variant products open @gorhom/bottom-sheet with variant stock counts
- Cart bar shows item count + total, appears when items added
- Cart expansion shows all items with quantity controls
- Review screen has: items list, discount (flat/%), payment method (4 options), currency selector
- Confirm writes sale + outbox atomically to SQLite
- Post-sale: toast shown, cart cleared, back to product grid
- Sales persist across app kill/restart (SQLite)
</success_criteria>

<output>
After completion, create `.planning/phases/02-mobile-pos-core/02-05-SUMMARY.md`
</output>
