MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/bandmerch?retryWrites=true&w=majority
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
PORT=5000
NODE_ENV=development

# Meta Conversions API (server-side pixel events)
# Generate access token at Events Manager → Pixel → Settings → Conversions API
META_PIXEL_ID=462106368687515
META_CAPI_ACCESS_TOKEN=
# Optional: set to the code shown in Events Manager → Test Events while verifying.
# Remove in production so events count toward the real dataset.
META_CAPI_TEST_EVENT_CODE=
